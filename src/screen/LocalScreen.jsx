import React from 'react'
import LocalContainer from '../containers/LocalContainer/LocalContainer'

const LocalScreen = (props) => {
    return (
        <LocalContainer {...props} />
    )
}

export default LocalScreen